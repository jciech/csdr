# macOS metadata
.DS_Store

# Build artifacts
csdr
nmux
ddcd
libcsdr.dylib
libcsdr.so
libcsdr.so.*
*.o
*.so
*.dylib

# Debug symbols
*.dSYM/

# Dump files (if still enabled)
dumpvect*.vect
libcsdr.disasm